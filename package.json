{
  "name": "j2cs-compiler",
  "version": "0.1.0",
  "private": true,
  "description": "AST-first JavaScript/TypeScript to C# compiler consuming j2cs semantic rules",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "compile": "node dist/compiler/cli.js",
    "test": "npm run build && node tools/run-tests.mjs",
    "test:unit": "npm run build && node tools/run-tests.mjs unit",
    "test:differential": "npm run build && node tools/run-tests.mjs differential",
    "validate:rules": "python rule-db/tools/validate_rules.py",
    "coverage:gaps": "python rule-db/tools/coverage_gap.py --top 10",
    "check": "npm test && npm run validate:rules && npm run coverage:gaps"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "typescript": "5.9.3"
  },
  "devDependencies": {
    "@types/node": "22.19.0"
  }
}
